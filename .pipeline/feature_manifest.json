{
  "_behavioral_specs": {
    "_global": [
      {
        "text": "## Summary\nBuild a greenfield single-container full-stack \"FaithfulC Recipe Box\": a TypeScript React (Vite + React Router) SPA served by a Node/Express + TypeScript REST API backed by PostgreSQL, with JWT-based `full_auth`, recipe CRUD/search/favorites, and 401/404/503 handling.\n\n## Scope\n### Files to modify\n- `Dockerfile` — replace the placeholder nginx static build with a multi-stage build: stage 1 builds the React app (`npm run build` → `client/dist`), stage 2 installs server deps + compiled TS, copies client build, runs Node on port 8080.\n- `README.md` — document stack, env vars, dev/build commands, demo credentials.\n\n### Files to create\n**Server (`server/`)**\n- `server/package.json`, `server/tsconfig.json` — Express + TypeScript deps (`express`, `pg`, `jsonwebtoken`, `bcryptjs`, `zod`, `dotenv`; dev: `typescript`, `@types/*`, `tsx`).\n- `server/src/index.ts` — app bootstrap: run migrations + seed, mount routers, serve `client/dist` static + SPA fallback, listen on `process.env.PORT || 8080`.\n- `server/src/db.ts` — `pg` Pool from `DATABASE_URL`; helper that throws a typed `DbUnavailableError` on connection failure.\n- `server/src/migrate.ts` — idempotent `CREATE TABLE IF NOT EXISTS` for `users` and `recipes` + indexes; seed demo user.\n- `server/src/auth/jwt.ts` — sign/verify JWT (HS256, `JWT_SECRET`).\n- `server/src/auth/middleware.ts` — `requireAuth` middleware → 401 if missing/invalid token.\n- `server/src/routes/auth.ts` — `POST /api/auth/login`, `POST /api/auth/signup`, `GET /api/auth/me`, `POST /api/auth/logout`.\n- `server/src/routes/recipes.ts` — list/create/get/update/delete/favorite endpoints (see plan).\n- `server/src/routes/health.ts` — `GET /api/health` (liveness), `GET /api/health/deep` (checks DB).\n- `server/src/middleware/errors.ts` — central error handler mapping `ZodError`→400, `DbUnavailableError`→503, not-found→404.\n- `server/src/validation.ts` — zod schemas for recipe + auth payloads.\n\n**Client (`client/`)**\n- `client/package.json`, `client/tsconfig.json`, `client/vite.config.ts` — React + Vite + React Router + TypeScript; dev proxy `/api` → `localhost:8080`; `base: '/faithful-e2e-c/'` to match the ingress path prefix.\n- `client/index.html`, `client/src/main.tsx`, `client/src/App.tsx` — router with `data.flow` nodes on every route.\n- `client/src/api/client.ts` — fetch wrapper attaching `Authorization: Bearer`, handling 401→redirect to `/login`, 503→error banner.\n- `client/src/auth/AuthContext.tsx`, `client/src/auth/RequireAuth.tsx` — token storage (localStorage) + route guard.\n- `client/src/components/Header.tsx` — displays \"FaithfulC\" on all authenticated pages.\n- `client/src/components/RecipeCard.tsx`, `RecipeForm.tsx`, `FavoriteToggle.tsx`, `EmptyState.tsx`, `ErrorBanner.tsx`.\n- `client/src/pages/LoginPage.tsx`, `SignupPage.tsx`, `RecipeListPage.tsx`, `RecipeDetailPage.tsx`, `NotFoundPage.tsx`.\n- `client/src/styles.css`.\n\n**Root**\n- `.dockerignore`, `.gitignore`, `.env.example` (`DATABASE_URL`, `JWT_SECRET`, `PORT`, `DEMO_USERNAME`, `DEMO_PASSWORD`).\n\n### Dependencies\n- Server: `express`, `pg`, `jsonwebtoken`, `bcryptjs`, `zod`, `dotenv`.\n- Client: `react`, `react-dom`, `react-router-dom`, `vite`, `typescript`.\n- Runtime: PostgreSQL (provided by the `postgresql` deployment; connection via `DATABASE_URL` in `app-secrets`).\n\n### Integrations\n- None (no third-party APIs/SDKs; PostgreSQL is first-party infrastructure).\n\n### Assumptions\n- **Stack chosen** (greenfield, no config): single-container Node/Express + TypeScript API serving a React/Vite SPA, matching the one-image-on-8080 deployment.\n- **Recipes are per-user** (owned by the authenticated user); list/search/favorites are scoped to `user_id`.\n- **Demo user seeded on startup** from `DEMO_USERNAME`/`DEMO_PASSWORD` env (default `demo` / `demo1234`); documented in README. These are the \"demo credentials\".\n- **Auth = `full_auth`**: JWT (localStorage), login + signup (first user → `admin`, others → `user`) + logout + guards. Public routes: `/login`, `/signup`, `/api/health`, `/api/health/deep`.\n- **`ingredients` and `steps` stored as text** (newline-delimited); title required non-empty.\n- SPA `base` path is `/faithful-e2e-c/` to match the ingress rewrite; API called at path-relative `api/`.\n- 401 responses trigger a client redirect to `/login` (satisfies \"unauthenticated access\" redirect).\n\n## Implementation Plan\n\n### Step 1: Database schema & seed\n**File:** `server/src/migrate.ts`, `server/src/db.ts`\n**Change:** Create `users(id serial pk, username text unique not null, password_hash text not null, role text not null default 'user', created_at timestamptz default now())` and `recipes(id uuid default gen_random_uuid() pk, user_id int references users(id), title text not null, ingredients text not null, steps text not null, favorite boolean not null default false, created_at, updated_at)`. Add `CREATE INDEX ON recipes (user_id)`, `CREATE INDEX ON recipes (user_id, favorite)`, and `CREATE INDEX ON recipes (user_id, lower(title))` for fast search (NFR p95 < 500ms). Seed demo user via bcrypt if absent. `db.ts` exposes a pool + query helper that maps connection errors to `DbUnavailableError`.\n\n### Step 2: Auth API + middleware\n**File:** `server/src/routes/auth.ts`, `server/src/auth/*`\n**Change:** `POST /api/auth/login` → verify bcrypt, return JWT + user (401 on bad creds). `POST /api/auth/signup` → create user (first user role `admin`), return JWT. `GET /api/auth/me` (guarded). `requireAuth` verifies `Authorization: Bearer` → 401 otherwise. Apply `requireAuth` to all `/api/recipes*` routes.\n\n### Step 3: Recipes API\n**File:** `server/src/routes/recipes.ts`, `server/src/validation.ts`\n**Change:**\n- `GET /api/recipes?q=&favorite=true` — scoped to `req.user.id`; `q` → `WHERE lower(title) LIKE lower('%'||$q||'%')`; `favorite=true` → `AND favorite`. Returns array ordered by `updated_at desc`.\n- `POST /api/recipes` — zod-validate title/ingredients/steps non-empty → 400 on missing; insert; return 201 card.\n- `GET /api/recipes/:id` — owner-scoped; 404 if not found.\n- `PUT /api/recipes/:id` — validate → 400; update; 404 if absent; return updated.\n- `PATCH /api/recipes/:id/favorite` — toggle/set `favorite`; return updated.\n- `DELETE /api/recipes/:id` — delete; 404 if absent; return 204.\nAll DB errors bubble to the 503 handler.\n\n### Step 4: Error handling & health\n**File:** `server/src/middleware/errors.ts`, `server/src/routes/health.ts`\n**Change:** Central handler: `ZodError`→400 `{error, fields}`; `DbUnavailableError`→503 `{error:\"Service Unavailable\"}`; unknown→500. `/api/health` returns 200 always; `/api/health/deep` runs `SELECT 1` → 503 if DB down.\n\n### Step 5: Server bootstrap & static serving\n**File:** `server/src/index.ts`\n**Change:** Run `migrate()` on boot (log + continue if DB down so liveness stays up), mount routers under `/api`, serve `client/dist` statically, SPA fallback to `index.html` for non-API GETs, listen on `PORT||8080`.\n\n### Step 6: Client routing & auth shell\n**File:** `client/src/App.tsx`, `client/src/auth/*`\n**Change:** Routes, each with a `data.flow` node for machine verification:\n- `/login` → LoginPage (public) — `flow: login`\n- `/signup` → SignupPage (public) — `flow: signup`\n- `/` → RecipeListPage (guarded), state bound to query params `?q=<search>&fav=1` and add-recipe modal `?modal=new` — `flow: recipe-list`\n- `/recipes/:id` → RecipeDetailPage (guarded), edit mode `?edit=1` — `flow: recipe-detail`\n- `*` → NotFoundPage — `flow: not-found`\n`RequireAuth` redirects to `/login` when no valid token; `AuthContext` stores JWT and exposes `login/logout/user`.\n\n### Step 7: Login & signup pages\n**File:** `client/src/pages/LoginPage.tsx`, `SignupPage.tsx`\n**Change:** Login form posts creds; on success store token → redirect `/`; on 401 show \"Invalid credentials\". Signup analogous. Pre-fill hint for demo credentials.\n\n### Step 8: Recipe list, search, favorites filter, add\n**File:** `client/src/pages/RecipeListPage.tsx`, components\n**Change:** Fetch `GET /api/recipes` with `q`/`favorite` from query params; render `RecipeCard` grid (title + favorite indicator). Search input updates `?q=` (debounced); favorites toggle updates `?fav=1`. Empty result → `EmptyState` message, no cards. \"Add recipe\" opens `?modal=new` with `RecipeForm`; submit posts, closes modal, refreshes list; inline 400 validation errors. Card favorite toggle → `PATCH .../favorite`.\n\n### Step 9: Recipe detail, edit, delete\n**File:** `client/src/pages/RecipeDetailPage.tsx`\n**Change:** Fetch `GET /api/recipes/:id`; 404 response → NotFound state. Show full title/ingredients/steps + favorite toggle. `?edit=1` renders editable `RecipeForm` → `PUT` (400 → inline errors). Delete button confirms → `DELETE` → redirect `/`.\n\n### Step 10: Header & global error/loading UX\n**File:** `client/src/components/Header.tsx`, `ErrorBanner.tsx`, `client/src/api/client.ts`\n**Change:** Header shows \"FaithfulC\" + logout on all authenticated pages. API client shows `ErrorBanner` (\"Service temporarily unavailable\") on 503 and redirects to `/login` on 401.\n\n### Step 11: Dockerfile & build wiring\n**File:** `Dockerfile`, `.dockerignore`\n**Change:** Multi-stage: (1) `node:20-alpine` builds `client` → `dist`; (2) builds `server` TS → `dist`; (3) runtime installs server prod deps, copies `server/dist` + `client/dist`, `EXPOSE 8080`, `CMD [\"node\",\"server/dist/index.js\"]`. Reads `DATABASE_URL`/`JWT_SECRET` from env (`app-secrets`).\n\n## Testing Strategy\n- **Auth:** login with demo creds → 200 + token + redirect; bad creds → 401 + error; unauthenticated `/api/recipes` → 401; unauthenticated page nav → redirect to `/login`.\n- **CRUD/validation:** create/list/get/update/delete round-trip; missing title/ingredients/steps on create and edit → 400, no persistence.\n- **Search/favorites:** `?q=` filters by title substring; no match → empty-state message, zero cards; `?fav=1` shows only favorites; toggle persists across reload.\n- **Not found:** GET unknown `/recipes/:id` → 404 state.\n- **503:** stop Postgres → load/save/search/delete return 503 + banner; `/api/health` stays 200, `/api/health/deep` → 503.\n- **NFR:** seed ~100 recipes, measure `GET /api/recipes` p95 < 500ms (index-backed).\n- **Build:** `docker build` succeeds; container serves SPA on 8080 and connects to Postgres.\n\n## Risks\n- **DB connectivity/secret:** app depends on `DATABASE_URL` in `app-secrets`; if absent the app stays live but all recipe ops return 503 (by design) — verify secret is provisioned.\n- **Ingress base path:** SPA must build with `base: /faithful-e2e-c/`; a mismatch breaks asset loading — verify against the ingress rewrite rule.\n- **gen_random_uuid():** requires `pgcrypto`/Postgres 13+; migration should `CREATE EXTENSION IF NOT EXISTS pgcrypto` (or use app-generated UUIDs) to avoid failure.\n- **First full integration:** greenfield — client, server, and DB are wired together for the first time; end-to-end smoke test before deploy.",
        "type": "architectural",
        "source": "plan",
        "rev": 0,
        "superseded": false
      }
    ]
  },
  "_component_requirements": {}
}