# Atlas Stack Status
Generated by scaffolder. Read this before writing any code.

## Verdict: EXISTING — preserve current stack

## Detected stack
- Runtime: NestJS (TypeScript) — scaffolded from template-backend; React + Vite (TypeScript) — scaffolded from template-web
- ORM: Prisma (PostgreSQL)
- Auth: Custom JWT (Bearer token via passport-jwt)
- Health checks: present at /api/v1/health (template default; plan targets /api/health)
- K8s service name: none detected

## Rules for all agents
- Do not migrate the ORM
- Do not replace the auth implementation
- Do not change the framework
- Only add features within the existing patterns
- Backend lives in `backend/` — NestJS with Prisma, port 8080
- Frontend lives in `frontend/` (inside `web/`) — React + Vite SPA served by the backend
- Single-container deployment: backend serves both API and static SPA assets
- Global API prefix is `/api` (override template default of `/api/v1` per plan)
- DATABASE_URL and JWT_SECRET must be provided via k8s `app-secrets`
