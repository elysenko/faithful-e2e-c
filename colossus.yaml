# colossus.yaml — generated by scaffolder, read by deploy agents.
# Do not delete. The deploy agents use this instead of framework auto-detection.
# {{IMAGE_NAME}} is a placeholder substituted by the deploy agent at build time.
# NOTE: plan calls for React/Vite SPA served by Express on port 8080 (single container).
# Coder should update framework/outputDir after implementing the actual stack.
framework: nextjs
outputDir: .next
baseHref: "/{{IMAGE_NAME}}/"
port: 8080
nginx: none
